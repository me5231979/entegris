/* Package configuration. The per-language SCORM build overwrites this file.
   defaultLang: language shown on first launch. lockLang: true hides the language dropdown. */
window.ENTG_CONFIG = { defaultLang: 'en', lockLang: false };
