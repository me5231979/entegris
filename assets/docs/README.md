# Course documents

The downloadable Playbook lives here as `Entegris-Great-Leader-Profile-Playbook-2026.pptx`. The download cards in every lesson point at that file name. Replace the file to update it, then rerun `scripts/build-single.py` so the published copy picks it up. The build script copies everything in this folder into the SCORM package and adds it to the manifest.
